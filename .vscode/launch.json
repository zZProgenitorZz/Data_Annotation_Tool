{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Run current file in src",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "cwd": "${workspaceFolder}/backend/src",
      "console": "integratedTerminal"
    }
  ]
}
